<form method="post" action="{{ path('app_api_radio_delete', {'id': api_radio.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir le supprimer ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ api_radio.id) }}">
	<button class="btn btnDelete">Supprimer</button>
</form>
