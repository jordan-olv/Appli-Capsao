<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('style.css') }}">
        
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
        {% block logout %}    
            {% if app.user %}
                <div class="mb-3">
                    You are logged in as {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Logout</a>
                </div>
            {% endif %}
        {% endblock %}
        {% block body %}{% endblock %}
        {% block menu %}
        <p>
            <a href="{{path('app_api_podcast_index')}}">podcast</a>
            <a href="{{path('app_api_radio_index')}}">radio</a>
            <a href="{{path('app_user_index')}}">user</a>
            <a href="{{path('app_register')}}">Créer user</a>
            <a href="{{path('app_login')}}">se connecterdqsdqsd</a>
            <a href="{{path('app_menu')}}">HOME</a>
            <a href="{{path('app_zone_texte', {'id': 1})}}">Texte</a>
            <a href="{{path('app_api_event', {'id': 1})}}">Event</a>
        </p>
        {% endblock %}
    </body>
</html>
