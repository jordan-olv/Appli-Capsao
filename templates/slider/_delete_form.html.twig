<form method="post" action="{{ path('app_slider_delete', {'id': slider.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir le supprimer ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ slider.id) }}">
	<button class="btn btnDelete">Supprimer</button>
</form>
